# Local frontend development example. Copy to frontend/.env.local when running
# Next.js directly outside Docker.
BACKEND_API_BASE=http://127.0.0.1:8000
AUDIT_API_KEY=replace-with-the-same-server-only-secret-as-backend
NEXT_PUBLIC_SITE_URL=http://localhost:3000
