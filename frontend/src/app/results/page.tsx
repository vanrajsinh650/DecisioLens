import { Suspense } from "react";

import ResultsPageClient from "@/features/results/ResultsPageClient";

export function generateMetadata() {
    return {
        title: "DecisioLens — AI Trust Report",
        description: "View a shared AI decision audit report generated by DecisioLens.",
        openGraph: {
            title: "DecisioLens Trust Report",
            description: "AI Decision Trust Score",
        },
    };
}

export default function ResultsPage() {
    return (
        <Suspense fallback={<div className="text-sm text-ink-200">Loading report...</div>}>
            <ResultsPageClient />
        </Suspense>
    );
}
